apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  annotations:
    argocd-image-updater.argoproj.io/image-list: demo=010438490109.dkr.ecr.ap-south-1.amazonaws.com/web-dev
    argocd-image-updater.argoproj.io/demo.update-strategy: latest
    argocd-image-updater.argoproj.io/demo.allow-tags: regexp:^v.*
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers:
      - name: demo
        image: 010438490109.dkr.ecr.ap-south-1.amazonaws.com/web-dev:v2
        ports:
        - containerPort: 80
